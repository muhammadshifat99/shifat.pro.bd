# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# superpowers workspace
.superpowers/

# local dev/AI tooling (kept local, not for the public repo)
.commandcode/
AGENTS.md
CLAUDE.md
/docs/

# working documents — drafts, exports and reports that inform the site but
# are not part of it. Ignored rather than merely left unstaged so a
# `git add -A` can't publish them.
NPC_AUTOMATORS_Agency_Overview_v1.pdf
shifat-seo-package.md
seo-aeo-geo-toolkit-template.md
seo-aeo-geo-toolkit.html
# PageSpeed downloads carry a volatile hash in the filename, so match the
# prefix rather than one exact name
pagespeed.web.dev_analysis_*.png
# screenshots saved for reference while working on SEO
google search preview*.png
